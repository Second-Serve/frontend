<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Secondserve"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.firebase.ml.vision"
            android:value="true" />

        <activity
            android:name=".LandingPageView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".GetStartedView"/>
        <activity android:name=".LoginView" />
        <activity android:name=".UserSignUpView" />
        <activity android:name=".RestaurantPageView" />
        <activity android:name=".RestaurantSearchView" />
        <activity android:name=".RestaurantSignUpView" />
        <activity android:name=".SaveLocationView" />
        <activity android:name=".SecondServeView" />
        <activity android:name=".RestaurantMainView"/>
        <activity android:name=".CheckoutView"/>
        <activity android:name=".PaymentView"/>


    </application>

</manifest>